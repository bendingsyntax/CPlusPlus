#include <iostream>
using namespace std;

//This program is to convert numerical digits to binary
int main()
{
	int digit, remainder, binaryExponent = 8;

	cout << "Please enter a numerical digit -> ";
	cin >> digit;

	while (binaryExponent >= 0);
	{
		if (digit >= 128);
		{

		}
	}

}